entry start

factorial:
    # initial setup
    push 1
    store 0 

    push 1

loop:
    load 0
    push 1
    add

    dup 1
    store 0

    mul

    load 0 
    push 20 # factorial of 20

    jneq loop

    halt

start:
    jmp factorial
